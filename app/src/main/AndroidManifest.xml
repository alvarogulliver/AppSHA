<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="appsha.usp.com.myappsha" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateNewPerson"
            android:label="@string/title_activity_create_new_person" >
        </activity>
        <activity
            android:name=".SelectPerson"
            android:label="@string/title_activity_select_person" >
        </activity>
        <activity
            android:name=".GenerateDiagnosticReport"
            android:label="@string/title_activity_generate_diagnostic_report" >
        </activity>
        <activity
            android:name=".CaptureData"
            android:label="@string/title_activity_capture_data" >
        </activity>
    </application>

</manifest>
